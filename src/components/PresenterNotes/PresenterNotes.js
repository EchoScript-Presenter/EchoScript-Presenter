import React from 'react';
import styled from 'styled-components';

const PresenterNotesContainer = styled.div`
  /* 스타일링 */
`;

function PresenterNotes({ onHighlight, highlightedContent }) {
  // PresenterNotes 컴포넌트의 로직과 렌더링

  return <PresenterNotesContainer>{/* Notes 내용 */}</PresenterNotesContainer>;
}

export default PresenterNotes;
